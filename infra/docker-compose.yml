version: '3.8'
services:
  api:
    build: ./services/api
    command: uvicorn services.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - 8000:8000
  go-svc:
    build: ./services/go
    ports:
      - 8081:8081
